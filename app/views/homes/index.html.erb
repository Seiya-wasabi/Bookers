<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>WebCampBackendNew</title>
</head>
<body>
  <h1 class ="title-head">Books</h1>
  <!--booksの一覧表示-->
  <div class ="container">
    <div class ="list1">
<p><strong>Title</strong></p>
<% @books.each do |b| %>
  <span><%= link_to b.title,"/homes/#{b.id}" %></span>
<% end %>
    </div>
    <div class ="list2">
<p><strong>Body</strong></p>
<% @books.each do |b| %>
  <span><%= link_to b.body,"/homes/#{b.id}" %></span>
<% end %>
    </div>
  </div>

  <h2>New book</h2>
  <%= form_with model: @book, url:'/homes',local:true do |f| %>
  <div class = "form">
  <p class = "text1">Title</p>
  <%= f.text_field :title %>
  </div>
  <div class = "form">
  <p class= "text1">Body</p>
  <%= f.text_area :body %>
  </div>
  <%= f.submit 'Create Book' %>
  <% end %>
</body>
</html>